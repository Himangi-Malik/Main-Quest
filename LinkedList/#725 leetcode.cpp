//samjh aagya tha kya karna, doosre type se answer nikaal bhi diya tha
//but it was taking too much time and slowing me down so pokaik puk puk
//i am skipping this but sahi socha tha maine remainder and dividend ke case mein thanks
class Solution {
public:
    int length(ListNode* head){
        int count = 0;
        while(head!= NULL){
            count++;
            head = head->next
        }
        cout << length << endl;
        return count;
    }
    vector<ListNode*> splitListToParts(ListNode* head, int k) {
        vector <ListNode*> ans;//final ans ka vector

        int full_length = length(head);
        int div = full_length/k;
        int rem = full_length % k;
        int count, check = 0;

        ListNode* curr = head; 
        ListNode* prev = NULL;
        
    while()

        
    }
};
